# 原应用运行与操作记录

2026-09-07，在隔离本地数据目录中运行原应用。使用其原始离线初始化程序生成12个纯合成案例，业务代码保持不变。前端构建通过，FastAPI 服务仅监听本机地址，评分 Provider 调用数为0。

| 对象 | 本次操作 | 实际结果 |
|---|---|---|
| 原应用前端与后端 | 安装锁定依赖、构建前端、启动本机服务 | 构建成功；仪表盘、材料、评分、复核和导出页可打开 |
| P005 失败案例 | 原界面创建同模型重试决定 | HTTP 201，决定记录新增；仅登记，未执行模型重试 |
| P004 低置信度案例 | 通过原存储接口新增演示工单，再由原界面创建采用决定 | HTTP 201，决定记录新增 |
| P004 采用结果 | 填写演示赛事 ID 后调用原采用接口 | HTTP 409，`REVIEW_APPLICATION_NOT_APPLICABLE`；未采用、未锁定 |
| M006 已锁定案例 | 查看预置采用、锁定状态；新增一条复核决定 | 原采用、锁定记录保持不变；界面人工调整不可用。新决定本身仍可登记 |
| P005、M006 权威导出判定 | 调用原结果推导接口 | `EXPORT_BLOCKED_BY_REVIEW`，未结案工单阻断导出；P005 还缺权威结果 |
| M006 恢复检查 | 调用不执行模型的恢复接口 | `already_completed`，`provider_called=false`；这一响应不单独证明锁定拦截 |
| 综合评分 Excel | 调用原文件导出接口 | HTTP 200，实际生成12行评分记录的工作簿 |

P004 原预置任务项为 `manual_review`，而采用执行入口要求 `review/pending`，因此这次采用被拒绝。本轮保留这一结果，没有修改任务状态来制造通过。

还尝试了 P001 抽检工单：按原契约，“随机抽检”与“采用已有评分”的组合被拒绝，随后成功记录“无问题驳回”决定；该决定未自动结束工单。一次 M006 人工调分请求因决定类型不适用返回 `REVIEW_DECISION_NOT_FOUND`，不能用它证明调分锁定拦截。

## 历史终态样例的根因

原离线初始化程序直接预置终态；正式采用入口只接受待复核状态。新案例通过原 `ScoringTaskCreator` 与 `PipelineTaskManager` 正常创建、启动并推进到 `review/pending`，原终态样例保持不变。结案由原 `ReviewCaseStore.transition_review_case` 合法转换并记录决定引用，不是手改文件。

## 两种导出

普通 Excel 导出读取旧版综合评分表；权威结果推导读取评分尝试、采用、锁定和复核状态。本轮已另外跑通同一新案例的采用、锁定、结案与权威 JSON 响应；两套出口仍是独立数据源，不声称 Excel 已接入该权威闭环。Word 文件也能生成，但本机渲染存在标题和表格跨页问题，因此未作为本轮展示附件。

## 新案例完整证据

新任务 `169a6495-c6e8-4d85-a1f9-1724cb720858`、工单 `22a77d3c-b733-431b-be9b-9df5406f8b9d` 的时间顺序和事件见[运行记录](run-evidence.json)。采用前的92分是合成预置；采用、锁定和结案是本轮实际操作。

[采用前截图](screenshots/live-before.png) · [采用锁定截图](screenshots/live-adopted-locked.png) · [结案截图](screenshots/live-resolved.png) · [权威结果逐字段表](authoritative-result.md)

原接口 JSON 原样保存；同案锁定后仍未结案的对照返回409，结案后200。任务项停在原接口对应的 `export/pending`：已得到权威结果响应，并未伪称整个流水线任务完成。UI采用时间字段沿用决定时间，因此实际采用先后以服务访问日志与事件顺序为准。

## 数据和验证边界

- P001、P004、P005、M006 是不同案例，不拼成一次运行轨迹。初始材料、分数、评语、失败与锁定状态均为预置。
- 本次实际执行的是构建、服务运行、页面操作、决定写入、接口判定和文件导出。截图由原页面直接捕获，没有重绘界面。
- 没有运行真实 Provider、模型效果评测、完整生产回归或多用户部署验收。历史测试数不作为本次结果。
- 原始应用代码、业务数据、配置、运行日志与私有 Git 历史不在公开仓库中。

[本次操作摘要 JSON](run-evidence.json) · [来源与公开范围](../EVIDENCE.md) · [返回应用展示](README.md)
